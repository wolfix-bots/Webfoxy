/* @module 0x2d9264b62c95b8eb9ef1b5e1c81219c3 */
/* 21f775b7f025a5b0ddcc542ee75356c3c3bdb7f0 */ import axios from 'axios'; export default { name: "7clouds", alias: ["7c", String.fromCharCode(102,111,120,121,55,99), String.fromCharCode(108,121,114,105,99,118,105,100,101,111), String.fromCharCode(107,97,114,97,111,107,101), String.fromCharCode(109,117,115,105,99,118,105,100,101,111)], description: "Download music videos from 7clouds with lyrics", category: String.fromCharCode(68,111,119,110,108,111,97,100,101,114), async execute(pz, oh, wn, qq, bw) { const chatId = oh.key.remoteJid; const sendReaction = async (emoji) => { try { await pz.sendMessage(chatId, { react: { text: emoji, key: oh.key } }); } catch {} }; try { const q = wn.join(' '); if (!q) { await sendReaction("❓"); await pz.sendMessage(chatId, { text: `*Usage:* ${qq}7clouds <song name>\n*Example:* ${qq}7clouds believer`, quoted: oh }); return; } await sendReaction("🎶"); const searchQueries = [ `${q} 7clouds`, `${q} 7clouds lyrics`, `${q} lyric video`, `${q} official lyric video`, `${q} official music video` ]; let videoData = null; let isFrom7clouds = false; for (const query of searchQueries) { try { console.log(`Trying search: ${query}`); let searchResponse = null; try { searchResponse = await axios.get( `https://apiskeith.vercel.app/search/yts?query=${encodeURIComponent(query)}`, { timeout: 8000 } ); } catch (e1) { searchResponse = await axios.get( `https://apiskeith.vercel.app/youtube/search?q=${encodeURIComponent(query)}`, { timeout: 8000 } ); } const videos = searchResponse?.data?.result || searchResponse?.data?.videos || []; if (videos.length > 0) { const sevenCloudsVideos = videos.filter(video => { const title = (video.title || '').toLowerCase(); const channel = (video.channel || '').toLowerCase(); const desc = (video.description || '').toLowerCase(); return ( title.includes('7clouds') || channel.includes('7clouds') || (title.includes('lyric') && (title.includes('7') || channel.includes('7'))) || (title.includes('lyrics') && title.includes('video')) || channel.includes('lyric video') ); }); if (sevenCloudsVideos.length > 0) { videoData = sevenCloudsVideos[0]; isFrom7clouds = true; console.log(`Found 7clouds video: ${videoData.title}`); break; } else if (query.includes('7clouds') && videos.length > 0) { videoData = videos[0]; console.log(`Found video (maybe 7clouds): ${videoData.title}`); break; } } } catch (searchError) { console.log(`Search failed for: ${query}`, searchError.message); continue; } } if (!videoData) { try { console.log('Trying direct YouTube search...'); const directSearch = await axios.get( `https://apiskeith.vercel.app/youtube/search?q=${encodeURIComponent(q + " official music video")}`, { timeout: 10000 } ); const videos = directSearch?.data?.videos || directSearch?.data?.result || []; if (videos.length > 0) { videoData = videos[0]; console.log(`Found general video: ${videoData.title}`); } } catch (directError) { console.log('Direct search failed:', directError.message); } } if (!videoData || !videoData.url) { await sendReaction("❌"); await pz.sendMessage(chatId, { text: `❌ No video found for "${q}"\nTry a different song title or check if it exists on YouTube`, quoted: oh }); return; } await sendReaction("✅"); await sendReaction("📥"); let downloadUrl = null; const videoId = videoData.url.includes('watch?v=') ? videoData.url.split('watch?v=')[1].split('&')[0] : null; if (videoId) { try { const ytDownload = await axios.get( `https://apiskeith.vercel.app/api/youtube/video?id=${videoId}`, { timeout: 30000 } ); if (ytDownload.data?.result || ytDownload.data?.url) { downloadUrl = ytDownload.data.result || ytDownload.data.url; } } catch {} if (!downloadUrl) { try { const altDownload = await axios.get( `https://apiskeith.vercel.app/download/video?url=${encodeURIComponent(videoData.url)}&quality=360`, { timeout: 30000 } ); if (altDownload.data?.result) downloadUrl = altDownload.data.result; } catch {} } if (!downloadUrl) { try { const anotherDownload = await axios.get( `https://apiskeith.vercel.app/api/youtube/videomp4?url=${encodeURIComponent(videoData.url)}`, { timeout: 30000 } ); if (anotherDownload.data?.url) downloadUrl = anotherDownload.data.url; } catch {} } } if (!downloadUrl) { downloadUrl = videoData.url; } await sendReaction("🚀"); const cleanTitle = (videoData.title || `${q} music video`) .replace(/[^\w\s.-]/gi, '') .substring(0, 50); setTimeout(async () => { try { const caption = isFrom7clouds ? `🎬 *7CLOUDS* - ${cleanTitle}\n🎤 Lyrics | 🎶 Music Video` : `🎬 *Music Video* - ${cleanTitle}\n📥 Downloaded via Bot`; await pz.sendMessage(chatId, { video: { url: downloadUrl, mimetype: "video/mp4" }, caption: caption, fileName: `${cleanTitle}.mp4`, gifPlayback: false }, { quoted: oh }); setTimeout(() => sendReaction("✅"), 1000); } catch (sendError) { console.error('Send error:', sendError); await sendReaction("❌"); await pz.sendMessage(chatId, { text: `📎 *Video Link:*\n${videoData.url}\n\nDownload manually or try again.`, quoted: oh }); } }, 500); } catch (error) { console.error('7clouds error:', error); await sendReaction("❌"); await pz.sendMessage(chatId, { text: `❌ Server error. Try: ${qq}ytmp4 ${wn.join(' ') || 'song'}`, quoted: oh }); } } };