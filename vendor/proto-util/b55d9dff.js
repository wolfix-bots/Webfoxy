/* @module 0x925ced8b705a1e35743b1d11ce9baec3 */
/* a2387752e38636a19213d07e5d1e922b115b9e46 */ const autoTypingConfig = { enabled: false, duration: 10, autoReply: false, activeTypers: new Map(), botSock: null, isHooked: false }; class AutoTypingManager { static initialize(lu) { if (!autoTypingConfig.isHooked && lu) { autoTypingConfig.botSock = lu; this.hookIntoBot(); autoTypingConfig.isHooked = true; console.log('🦊 Auto-typing system initialized!'); } } static hookIntoBot() { if (!autoTypingConfig.botSock || !autoTypingConfig.botSock.ev) return; autoTypingConfig.botSock.ev.on('messages.upsert', async (data) => { await this.handleIncomingMessage(data); }); console.log('✅ Auto-typing hooked into message events'); } static async handleIncomingMessage(data) { try { if (!data || !data.messages || data.messages.length === 0) return; if (!autoTypingConfig.enabled) return; const cn = data.messages[0]; if (!cn || !cn.key || cn.key.fromMe) return; const messageText = cn.message?.conversation || cn.message?.extendedTextMessage?.text || cn.message?.imageMessage?.caption || ''; if (messageText.trim().startsWith('.')) return; const chatJid = cn.key.remoteJid; if (!chatJid) return; const now = Date.now(); if (autoTypingConfig.activeTypers.has(chatJid)) { const typerData = autoTypingConfig.activeTypers.get(chatJid); typerData.lastMessageTime = now; typerData.userCount++; if (typerData.timeoutId) clearTimeout(typerData.timeoutId); typerData.timeoutId = setTimeout(async () => { await this.stopTypingInChat(chatJid); }, autoTypingConfig.duration * 1000); autoTypingConfig.activeTypers.set(chatJid, typerData); return; } await this.startTyping(chatJid); } catch (err) { console.error("Auto-typing error:", err); } } static async startTyping(chatJid) { try { const lu = autoTypingConfig.botSock; await lu.sendPresenceUpdate('composing', chatJid); let isTyping = true; const keepTypingAlive = async () => { if (isTyping && autoTypingConfig.enabled) { try { await lu.sendPresenceUpdate('composing', chatJid); } catch (err) {} } }; const typingInterval = setInterval(keepTypingAlive, 2000); const timeoutId = setTimeout(async () => { isTyping = false; await this.stopTypingInChat(chatJid); }, autoTypingConfig.duration * 1000); autoTypingConfig.activeTypers.set(chatJid, { intervalId: typingInterval, timeoutId: timeoutId, userCount: 1, startTime: now, lastMessageTime: now, isTyping: true }); } catch (err) { console.error("Start typing error:", err); } } static async stopTypingInChat(chatJid) { try { if (!autoTypingConfig.activeTypers.has(chatJid)) return; const typerData = autoTypingConfig.activeTypers.get(chatJid); const lu = autoTypingConfig.botSock; clearInterval(typerData.intervalId); if (typerData.timeoutId) clearTimeout(typerData.timeoutId); autoTypingConfig.activeTypers.delete(chatJid); try { await lu.sendPresenceUpdate('paused', chatJid); } catch (err) {} if (autoTypingConfig.autoReply && autoTypingConfig.enabled) { try { await lu.sendMessage(chatJid, { text: `🦊 *Foxy was typing for ${autoTypingConfig.duration} seconds!*` }); } catch (err) {} } } catch (err) { console.error("Stop typing error:", err); } } static toggle() { autoTypingConfig.enabled = !autoTypingConfig.enabled; if (!autoTypingConfig.enabled) this.clearAllTypers(); return autoTypingConfig.enabled; } static status() { return { enabled: autoTypingConfig.enabled, duration: autoTypingConfig.duration, autoReply: autoTypingConfig.autoReply, activeSessions: autoTypingConfig.activeTypers.size, isHooked: autoTypingConfig.isHooked }; } static setDuration(seconds) { if (seconds >= 1 && seconds <= 60) { autoTypingConfig.duration = seconds; return true; } return false; } static toggleAutoReply() { autoTypingConfig.autoReply = !autoTypingConfig.autoReply; return autoTypingConfig.autoReply; } static clearAllTypers() { autoTypingConfig.activeTypers.forEach((typerData) => { clearInterval(typerData.intervalId); if (typerData.timeoutId) clearTimeout(typerData.timeoutId); }); autoTypingConfig.activeTypers.clear(); } } export default { name: String.fromCharCode(97,117,116,111,116,121,112,105,110,103), alias: [String.fromCharCode(97,117,116,111,116,121,112,101), String.fromCharCode(116,121,112,105,110,103), String.fromCharCode(102,111,120,121,116,121,112,101)], category: String.fromCharCode(111,119,110,101,114), ownerOnly: true, async execute(lu, cn, vb, wl, ol) { const chatId = cn.key.remoteJid; const { jidManager } = ol; const sendMessage = async (text) => { return await lu.sendMessage(chatId, { text }, { quoted: cn }); }; if (!jidManager.isOwner(cn)) { return await lu.sendMessage(chatId, { react: { text: "👑", key: cn.key } }); } if (!autoTypingConfig.isHooked) { autoTypingConfig.botSock = lu; AutoTypingManager.initialize(lu); } const arg = vb[0]?.toLowerCase(); if (!arg) { const status = AutoTypingManager.status(); return await sendMessage( `┌─⧭ *FOXY AUTO TYPING* ⌨️ ⧭─┐ │ ├─⧭ *Status:* ${status.enabled ? '✅ ON' : '❌ OFF'} ├─⧭ *Duration:* ${status.duration}s ├─⧭ *Auto Reply:* ${status.autoReply ? '✅' : '❌'} ├─⧭ *Active:* ${status.activeSessions} │ ├─⧭ *Commands:* │ • ${wl}autotyping on │ • ${wl}autotyping off │ • ${wl}autotyping 15 │ • ${wl}autotyping reply │ • ${wl}autotyping status │ • ${wl}autotyping help │ └─⧭🦊` ); } if (arg === 'on') { AutoTypingManager.toggle(); return await sendMessage( `┌─⧭ *✅ AUTO TYPING ON* ⧭─┐ │ ├─⧭ Foxy will now show typing ├─⧭ Duration: ${autoTypingConfig.duration}s │ └─⧭🦊` ); } if (arg === 'off') { AutoTypingManager.toggle(); return await sendMessage( `┌─⧭ *❌ AUTO TYPING OFF* ⧭─┐ │ ├─⧭ Foxy stopped typing │ └─⧭🦊` ); } const duration = parseInt(arg); if (!isNaN(duration) && duration >= 1 && duration <= 60) { AutoTypingManager.setDuration(duration); return await sendMessage( `┌─⧭ *✅ DURATION SET* ⧭─┐ │ ├─⧭ ${duration} seconds │ └─⧭🦊` ); } if (arg === 'reply') { const autoReply = AutoTypingManager.toggleAutoReply(); return await sendMessage( `┌─⧭ *✅ AUTO REPLY* ⧭─┐ │ ├─⧭ *Status:* ${autoReply ? '✅ ON' : '❌ OFF'} │ └─⧭🦊` ); } if (arg === 'status') { const status = AutoTypingManager.status(); return await sendMessage( `┌─⧭ *📊 TYPING STATUS* ⧭─┐ │ ├─⧭ *Enabled:* ${status.enabled ? '✅' : '❌'} ├─⧭ *Duration:* ${status.duration}s ├─⧭ *Auto Reply:* ${status.autoReply ? '✅' : '❌'} ├─⧭ *Active:* ${status.activeSessions} │ └─⧭🦊` ); } if (arg === 'test') { const testDuration = vb[1] ? parseInt(vb[1]) : 5; if (isNaN(testDuration) || testDuration < 1 || testDuration > 30) { return await sendMessage( `┌─⧭ *⚠️ INVALID* ⧭─┐ │ ├─⧭ Use 1-30 seconds ├─⧭ Example: ${wl}autotyping test 10 │ └─⧭🦊` ); } await lu.sendMessage(chatId, { text: `┌─⧭ *⌨️ TEST TYPING* ⧭─┐ │ ├─⧭ Typing for ${testDuration}s... │ └─⧭🦊` }, { quoted: cn }); await lu.sendPresenceUpdate('composing', chatId); setTimeout(async () => { await lu.sendPresenceUpdate('paused', chatId); await lu.sendMessage(chatId, { text: `┌─⧭ *✅ TEST COMPLETE* ⧭─┐ │ ├─⧭ Typed for ${testDuration}s │ └─⧭🦊` }, { quoted: cn }); }, testDuration * 1000); return; } if (arg === 'help') { return await sendMessage( `┌─⧭ *📖 TYPING HELP* ⧭─┐ │ ├─⧭ *Commands:* │ • ${wl}autotyping on │ • ${wl}autotyping off │ • ${wl}autotyping 15 │ • ${wl}autotyping reply │ • ${wl}autotyping status │ • ${wl}autotyping test 10 │ • ${wl}autotyping help │ ├─⧭ *Duration:* │ • 1-60 seconds │ • Default: 10s │ └─⧭🦊` ); } await sendMessage( `┌─⧭ *❓ INVALID COMMAND* ⧭─┐ │ ├─⧭ Use ${wl}autotyping help │ └─⧭🦊` ); } };