/* @module 0x573423a41164e3929a61f13c02c6cec0 */
import { writeFileSync, readFileSync, existsSync } from 'fs'; import { join } from 'path'; export default { name: 'setprefix', alias: ['prefix'], category: 'owner', description: 'Change bot prefix (use "none" for no prefix)', ownerOnly: true, async execute(sock, msg, args, PREFIX, extra) { const chatId = msg.key.remoteJid; if (!args[0]) { return sock.sendMessage(chatId, { text: `┌─⧭ *SET PREFIX* ⧭─┐ │ ├─⧭ *Current Prefix:* │ "${PREFIX || 'none'}" │ ├─⧭ *Usage:* │ ${PREFIX}setprefix <new_prefix> │ ├─⧭ *Examples:* │ ${PREFIX}setprefix ! │ ${PREFIX}setprefix >> │ ${PREFIX}setprefix none │ ├─⧭ *Note:* │ Max 5 characters │ Use "none" to remove prefix │ └─⧭🦊` }, { quoted: msg }); } const input = args[0].trim(); if (input.toLowerCase() === 'none') { const newPrefix = ''; // Empty string for no prefix try { const prefixFile = join(process.cwd(), 'prefix_config.json'); let config = {}; if (existsSync(prefixFile)) { try { config = JSON.parse(readFileSync(prefixFile, 'utf8')); } catch (e) { config = {}; } } const oldPrefix = config.prefix || PREFIX || '.'; const displayOld = oldPrefix || 'none'; config.prefix = ''; config.updatedAt = new Date().toISOString(); config.updatedBy = extra?.OWNER_NUMBER || extra?.ownerNumber || 'unknown'; config.previousPrefix = oldPrefix; config.hasPrefix = false; config.isNoPrefix = true; writeFileSync(prefixFile, JSON.stringify(config, null, 2)); let updateSuccess = false; if (typeof extra?.updatePrefix === 'function') { const result = extra.updatePrefix(''); updateSuccess = result && result.success; } if (typeof global !== 'undefined') { global.CURRENT_PREFIX = ''; global.prefix = ''; updateSuccess = true; } process.env.PREFIX = ''; await sock.sendMessage(chatId, { text: `┌─⧭ *PREFIX REMOVED* ⧭─┐ │ ├─⧭ *Old:* "${displayOld}" ├─⧭ *New:* No prefix required! │ │ ✅ Commands now work WITHOUT prefix! │ Example: ping (instead of .ping) │ └─⧭🦊` }, { quoted: msg }); } catch (error) { await sock.sendMessage(chatId, { text: `┌─⧭ *ERROR* ⧭─┐ │ ├─⧭ ${error.message} │ └─⧭🦊` }, { quoted: msg }); } return; // Stop here for "none" case } const newPrefix = input; if (newPrefix.length > 5) { return sock.sendMessage(chatId, { text: `┌─⧭ *ERROR* ⧭─┐ │ ├─⧭ Prefix too long! │ Max 5 characters. │ ├─⧭ *Examples:* │ . ! # >> wolf │ └─⧭🦊` }, { quoted: msg }); } if (newPrefix.length === 0) { return sock.sendMessage(chatId, { text: `┌─⧭ *ERROR* ⧭─┐ │ ├─⧭ Prefix cannot be empty! │ Use "none" to remove prefix. │ └─⧭🦊` }, { quoted: msg }); } if (newPrefix.match(/[\\\/<>]/)) { return sock.sendMessage(chatId, { text: `┌─⧭ *ERROR* ⧭─┐ │ ├─⧭ Avoid these characters: │ \\ / < > │ └─⧭🦊` }, { quoted: msg }); } try { const prefixFile = join(process.cwd(), 'prefix_config.json'); let config = {}; if (existsSync(prefixFile)) { try { config = JSON.parse(readFileSync(prefixFile, 'utf8')); } catch (e) { config = {}; } } const oldPrefix = config.prefix || PREFIX || '.'; const displayOld = oldPrefix || 'none'; config.prefix = newPrefix; config.updatedAt = new Date().toISOString(); config.updatedBy = extra?.OWNER_NUMBER || extra?.ownerNumber || 'unknown'; config.previousPrefix = oldPrefix; config.hasPrefix = true; config.isNoPrefix = false; writeFileSync(prefixFile, JSON.stringify(config, null, 2)); let updateSuccess = false; if (typeof extra?.updatePrefix === 'function') { const result = extra.updatePrefix(newPrefix); updateSuccess = result && result.success; } if (typeof global !== 'undefined') { global.CURRENT_PREFIX = newPrefix; global.prefix = newPrefix; updateSuccess = true; } process.env.PREFIX = newPrefix; await sock.sendMessage(chatId, { text: `┌─⧭ *PREFIX UPDATED* ⧭─┐ │ ├─⧭ *Old:* "${displayOld}" ├─⧭ *New:* "${newPrefix}" │ │ ✅ Try: ${newPrefix}ping │ └─⧭🦊` }, { quoted: msg }); } catch (error) { await sock.sendMessage(chatId, { text: `┌─⧭ *ERROR* ⧭─┐ │ ├─⧭ ${error.message} │ └─⧭🦊` }, { quoted: msg }); } } }; console.log('🔧 Setprefix module loaded');